version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(':crew')
    compile project(':server')

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
